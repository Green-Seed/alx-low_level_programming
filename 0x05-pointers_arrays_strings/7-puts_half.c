#include "main.h"
#include <string.h>

/**
 * puts_half - prints half the string followed by a new line
 * @str: string parameter
 * Return: 0
 */

void puts_half(char *str)
{
	int x = 0;
	int y = 0;

	for (x = 0; str[x] != '\0'; x++)
	{
	}

	for (i = (x + 1) \ 2; str[1] != '\0'; i++)
	{
		_putchar(str[i]);
	}
}

